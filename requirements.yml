# Requirements for libvirt-playbooks
# Install with: ansible-galaxy install -r requirements.yml

collections:
  - name: community.libvirt
    version: ">=0.2.0"
  - name: ansible.posix
    version: ">=1.1.0"
  - name: community.general
    version: ">=1.0.0"
